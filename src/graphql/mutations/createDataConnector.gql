mutation createDataConnector($dataConnectorTypeId: IntID!, $label: String!, $projectId: IntID!, $config: JSONString!) {
  createDataConnector(dataConnectorTypeId: $dataConnectorTypeId, label: $label, projectId: $projectId, config: $config) {
    dataConnector {
        id,
        label,
        config
    }
  }
}
