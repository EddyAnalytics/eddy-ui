#import "../fragments/dataConnectorConfigFragment.gql"

mutation createDataConnector($configId: IntID!, $name: String!) {
  createDebeziumConnector(configId: $configId, name: $name) {
    debeziumConnector {
        id,
        name,
        config {
            ...dataConnectorConfigFragment
        }
    }
  }
}
