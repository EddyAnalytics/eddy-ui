mutation createDataConnector($id: IntID!, $label: String!, $config: JSONString!) {
  createDataConnector(id: $id, label: $label, config: $config) {
    dataConnector {
        id,
        label,
        config
    }
  }
}
